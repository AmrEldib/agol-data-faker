{
  "id": "userContent",
  "type": "object",
  "description": "Schema for response of User Content Request http://resources.arcgis.com/en/help/arcgis-rest-api/index.html#/User_Content/02r30000007p000000/",
  "title": "userContent",
  "properties": {
    "username": {
      "id": "username",
      "type": "string",
      "description": "The username of the content",
      "chance": "first"
    },
    "currentFolder": {
      "$ref": "folder"
    },
    "items": {
      "id": "items",
      "type": "array",
      "description": "The list of the user's items. See Item for response properties for more details",
      "minLength": 0,
      "items": {
        "$ref": "item"
      }
    }
  },
  "required": [ "username", "currentFolder", "items" ]
}
